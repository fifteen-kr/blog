---js
// <script>
const pagination = {
	data: "collections",
	size: 1,
	alias: "tag",
	filter: ["all", "posts"],
};

const eleventyExcludeFromCollections = true;

const eleventyComputed = {
	title: "Posts tagged '{{ tag }}'",
	permalink: function(data){ return `/tag/${this.slugify(data.tag)}/`; },
};
---
<h1>Posts tagged “{{ tag }}”</h1>

{% set posts = collections[tag] %}
{% include "posts.njk" %}

<p class="see-all-tags">See <a href="tag-list.njk">all tags</a>.</p>